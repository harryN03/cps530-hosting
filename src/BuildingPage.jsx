import React from 'react';

const BuildingPage = () => {
  return (
    <div>
      <h2>Building the Page</h2>
      <p>
        The webpage is composed of several React components, each responsible for a specific part of the UI.
        Components are structured and organized to maintain a clean and modular codebase.
      </p>
      <p>In particular, this webpage contains 5 different components (Introduction, Installation, BuildingPage, Difficulties, Solutions).</p>
      <p>After determine all components, we need to implement each component one by one and add them up together in the App.js file (which is our main file).</p>
    </div>
  );
};

export default BuildingPage;